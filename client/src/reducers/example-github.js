export const INITIAL_STATE = {
  response: {
    body: "Submit a query above! End default message.",
  },
};

export const reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
