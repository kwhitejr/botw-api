import React from "react";

import Header from "../../components/header";

const title = "About The Things";

const About = () => (
  <div>
    <Header title={title} />
    <p>This is the about page</p>
  </div>
);

export default About;
